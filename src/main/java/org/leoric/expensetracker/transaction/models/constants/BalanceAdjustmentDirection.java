package org.leoric.expensetracker.transaction.models.constants;

// this one is if i manually correct balance, i need to do that thru transaction. but i need to know if transaction is to plus or minus direction
public enum BalanceAdjustmentDirection {
	DEDUCTION,
	ADDITION
}